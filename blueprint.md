# Blueprint for Formspree Contact Form

## Project Overview
This project is a simple web page with basic HTML, CSS, and JavaScript.

## Implemented Features
- Initial "Hello, world!" page.

## Current Task: Implement a Formspree Contact Form

### Objective
To create a simple contact form that allows users to send inquiries via Formspree.

### Plan
1.  **Create `blueprint.md`**: Document the project overview, features, and current plan. (Already in progress)
2.  **Modify `index.html`**:
    *   Add a `<form>` element with `method="POST"` and `action="https://formspree.io/f/mlgogndn"`.
    *   Include input fields for "Name" (`name="name"`), "Email" (`name="_replyto"`, type="email"), and a "Message" (`name="message"`) textarea.
    *   Add a submit button.
    *   Ensure `style.css` is linked.
3.  **Modify `style.css`**:
    *   Add basic styling for the form elements (e.g., `form`, `input`, `textarea`, `button`).
    *   Improve overall page aesthetics.
